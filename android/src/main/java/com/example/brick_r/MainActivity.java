package com.example.brick_r;

import io.flutter.embedding.android.FlutterActivity;

public class MainActivity extends FlutterActivity {
    // v2 embedding — no registerWith() or onCreate() overrides
}

