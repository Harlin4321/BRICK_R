workflows:
  flutter-release-workflow:
    name: Build Android Release APK
    max_build_duration: 60
    environment:
      flutter: 3.35.5  # Flutter 3.35.5 with Dart 3.9.2
      vars:
        PACKAGE_NAME: "com.example.brick_r"

    scripts:
      - name: 🔹 Fully clean caches and old builds
        script: |
          echo "Clearing plugin cache and old builds..."
          rm -rf ~/.pub-cache
          rm -rf android/.gradle
          rm -rf android/app/build
          rm -rf build
          flutter clean

      - name: 🔹 Get packages
        script: |
          flutter pub get

      - name: 🔹 Build release APK
        script: |
          flutter build apk --release --build-name=1.0.0 --build-number=1

    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk

    publishing:
      email:
        recipients:
          - your-kr7013650812@gmail.com
          
          
          
          
          
          
          
          
  
      
          
          
